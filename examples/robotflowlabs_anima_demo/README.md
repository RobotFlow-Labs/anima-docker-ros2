# RobotFlowLabs ANIMA Demo Package

This package is the starter ROS 2 example for the public ANIMA workspace.

Build it inside ANIMA:

```bash
cd /workspaces/anima
colcon build
source install/setup.bash
ros2 run robotflowlabs_anima_demo hello_anima
```
